apiVersion: v1
kind: Secret
metadata:
  name: nextcloud-secrets
  namespace: nextcloud-test
type: Opaque
stringData:
  POSTGRES_PASSWORD: REPLACE_ME
  NEXTCLOUD_ADMIN_PASSWORD: REPLACE_ME
  REDIS_PASSWORD: REPLACE_ME
  REDIS_HOST_PASSWORD: REPLACE_ME
  SMTP_NAME: REPLACE_ME
  SMTP_PASSWORD: REPLACE_ME
  # Read by the before-starting hook via `occ user_oidc:provider --clientsecret-env`.
  OIDC_CLIENT_SECRET: REPLACE_ME
